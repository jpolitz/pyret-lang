{
  "name": "pyret-browser-test",
  "version": "0.0.0",
  "private": true,
  "description": "Run code.pyret.org's editor assertions against the embed API's embedded instances and the vscode extension's webviews, via one Playwright runner.",
  "scripts": {
    "cpo": "node run.js --env=cpo",
    "embed": "node run.js --env=embed",
    "vscode": "node run.js --env=vscode",
    "all": "./run-all.sh"
  },
  "devDependencies": {
    "@vscode/test-web": "^0.0.81",
    "playwright": "^1.49.1"
  }
}
