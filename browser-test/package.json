{
  "name": "pyret-browser-test",
  "version": "0.0.0",
  "private": true,
  "description": "Run code.pyret.org's editor assertions (node:test + node:assert) against the embed API's embedded instances and the vscode extension's webviews, via one Playwright runner.",
  "scripts": {
    "cpo": "node run.js --env=cpo",
    "embed": "node run.js --env=embed",
    "embed-static": "node run.js --env=embed-static",
    "vscode": "node run.js --env=vscode",
    "vscode-ovsx": "node run.js --env=vscode-ovsx",
    "cpo-mocha": "cd cpo && mocha test/embed.js test/image-equality.js test/number.js test/pyret.js test/shareUrls.js test/static-pages.js test/world.js",
    "all": "make matrix"
  },
  "//": "Filter with --grep (regex over suite + test names), e.g. `npm run embed -- --grep tables`.",
  "devDependencies": {
    "@vscode/test-web": "^0.0.81",
    "chromedriver": "^148.0.4",
    "mocha": "^11.7.6",
    "playwright": "^1.49.1",
    "q": "~1.4.1",
    "selenium-webdriver": "^3.6.0"
  }
}
